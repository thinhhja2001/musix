import 'package:flutter/material.dart';

class WeeklyAlbumWidget extends StatelessWidget {
  const WeeklyAlbumWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
