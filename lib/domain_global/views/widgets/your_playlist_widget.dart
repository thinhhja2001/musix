import 'package:flutter/material.dart';

class YourPlaylistWidget extends StatelessWidget {
  const YourPlaylistWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
