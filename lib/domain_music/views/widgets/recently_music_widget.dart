import 'package:flutter/material.dart';

class RecentlyMusicWidget extends StatelessWidget {
  const RecentlyMusicWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
